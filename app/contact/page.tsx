import Box from "@/components/elements/box";
import { Heading, SectionTitle } from "@/components/elements/title";

export default function Contact() {
  return (
    <main>
      <section className="container mx-auto my-36">
        <SectionTitle title={"Contact Us"} />
   
      </section>
    </main>
  );
}
